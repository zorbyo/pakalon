You are executing the /phase-5 command for Pakalon.

## Task
Start Phase 5: Deployment & Integration. Push to GitHub and deploy to cloud platform.

## Prerequisites
- Phase 3 and Phase 4 must be completed
- Check .pakalon-agents/ai-agents/phase-4/ exists
- Security issues should be resolved
- If not complete, inform user to complete previous phases

## Instructions
1. Read Phase 1-4 documents to understand the application
2. Prepare for deployment:

### Step 1: GitHub Repository
1. Create GitHub repository (if not exists)
2. Initialize git if needed
3. Create .gitignore
4. Create README.md
5. Push code to GitHub
6. Set up branch protection rules

### Step 2: Cloud Platform Selection
Ask user: "Which cloud platform would you like to deploy to?"
Options:
1. **AWS** (Amazon Web Services)
   - EC2, ECS, Lambda, S3, RDS
   - Requires AWS credentials
   
2. **DigitalOcean**
   - App Platform, Droplets, Managed Databases
   - Requires DO API token
   
3. **Azure** (Microsoft Azure)
   - App Service, Functions, Cosmos DB
   - Requires Azure credentials
   
4. **GCP** (Google Cloud Platform)
   - Cloud Run, Cloud Functions, Cloud SQL
   - Requires GCP service account
   
5. **Vercel** (Recommended for Next.js)
   - Automatic deployments from GitHub
   - Requires Vercel account
   
6. **None** (Skip deployment)

### Step 3: Deployment Configuration
Based on platform selection:
- Create deployment configuration files
- Set up CI/CD pipeline (GitHub Actions)
- Configure environment variables
- Set up database migrations
- Configure domain and SSL

### Step 4: Deploy Application
1. Run deployment scripts
2. Verify deployment
3. Run smoke tests
4. Monitor for issues

## Files to Create
1. phase-5/phase-5.md - Deployment summary
2. .github/workflows/deploy.yml - CI/CD pipeline
3. docker-compose.yml (if using Docker deployment)
4. Deployment platform configs (vercel.json, app.yaml, etc.)

## GitHub Actions Workflow
```yaml
name: Deploy
on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
      - run: npm ci
      - run: npm test
      - run: npm run build
      - name: Deploy
        # Platform-specific deployment step
```

## Critical Rules
- Never commit secrets or credentials
- Use environment variables for sensitive data
- Set up proper IAM roles and permissions
- Enable monitoring and logging
- Document deployment process
