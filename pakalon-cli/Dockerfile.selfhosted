FROM oven/bun:1

WORKDIR /app

COPY package.json bun.lock ./
RUN bun install --frozen-lockfile || bun install

COPY . .
RUN bun run build

ENV PAKALON_MODE=selfhosted
ENV PAKALON_LOCAL_DB=/root/.pakalon/local.db

CMD ["bun", "dist/index.js"]
